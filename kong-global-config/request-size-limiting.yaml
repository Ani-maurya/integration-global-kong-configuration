_format_version: "3.0"
plugins:
- config:
    allowed_payload_size: 100
    require_content_length: false
    size_unit: kilobytes
  enabled: true
  name: request-size-limiting
  protocols:
  - grpc
  - grpcs
  - http
  - https
